<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.MaterialComponents.Light">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>


    <style name="MyStateViewTheme" parent="StateViewTheme">
        <item name="stateBackgroundColor">@color/col0</item>
        <item name="stateActionButtonCornerRadius">8dp</item>
    </style>

    <style name="MyStateTitleAppearance" parent="StateViewTheme.TitleTextAppearance">
        <item name="android:textColor">#000000</item>
        <item name="android:fontFamily">@font/lobster_regular</item>
    </style>

    <style name="MyDescrAppearance" parent="StateViewTheme.DescriptionTextAppearance">
        <item name="android:textColor">#000000</item>
    </style>

</resources>
